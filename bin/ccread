#!/usr/bin/env php
<?php

require 'vendor/autoload.php';

setlocale(LC_MONETARY, 'en_US');

$reader = new Crad\CardReader();

while ($line = fgets(STDIN)) {
    try {
        $reader->read($line);
    } catch (Exception $e) {
        echo "Error: {$e->getMessage()}\n";
    }
}

echo "all done\n";